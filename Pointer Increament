#include<stdio.h>
int main()
{
    // int marks[]={89,59,96,78,99} ,i;
    // // 
    // // for(i=0;i<5;i++)
    // //   {
    // //       printf("Enter the marks of %d:",i+1);
    // //       scanf("%d",&marks[i]);
    // //   }
    
    // // for(i=0;i<5;i++)
    // //   {
    // //       printf("Student marks is: %d\n",i+1,marks[]);
    // //   }
    
    // printf("the value of marks 1  is %d\n",marks[0]);
    // printf("the value of marks 2 is %d\n",marks[1]);
    // printf("the value of marks 3 is %d\n",marks[2]);
    // printf("the value of marks 4 is %d\n",marks[3]);
    // printf("the value of marks 5 is %d\n",marks[4]);
    // printf("kya Baat hai bahut acche marks aaye hai tumhare...");
    int a = 8;
    
    int *ptr = &a;
    
    printf("The address of a is %u \n",ptr);
    
    ptr += 3;
    
    printf("The address of a is %u\n",ptr);
    
    
    
    return 0;
}
