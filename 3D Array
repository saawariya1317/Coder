#include<stdio.h>
#include<stdlib.h>

int main()
{
    // int arr[2][3] = {   {1,2,3},{4,5,6} };
    // int *b = &arr[0];
    
    // printf("%d\n",*b);
    // ++b;
    // printf("%d\n",*b);
    
    // for(int i=0;i<5;i++){
    //     printf("The value of student no %d is %d\n",i+1,*b);
    //     b++;
    // }
    
    
    // printf("%d",arr[1][1]);     //*******Array index starts with zero******
    
    int arr[2][3][4];
    for(int i=0;i<2;i++){
        for(int j=0;j<3;j++){
            for(int k=0;k<4;k++){
                
            printf("Enter value of arr[%d][%d][%d]\n",i,j,k);
            scanf("%d",&arr[i][j][k]);
            
            }
        }
    }
    
    for(int i=0;i<2;i++){
        for(int j=0;j<3;j++){
            for(int k=0;k<4;k++){
            printf("The value of arr[%d][%d][%d] is:  ****%d*****\n",i,j,k,arr[i][j][k]);
        }
    }
    }
    
    return 0;
}
